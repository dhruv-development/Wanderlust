name: CI for flask-app

on: 
  push:
    branches: main
  pull_request:
    branches: main

jobs: 
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run docker
        run: |
          cd python/flask-app
          docker stop flask-app-container
